server:
  port: 8080

spring:
  application:
    name: game-mng-service
  main:
    allow-bean-definition-overriding: true
  profiles:
    active:
      on-profile: dev
  cloud:
    discovery:
      enabled: false
    loadbalancer:
      enabled: false
  redis:
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 2
    #port: 16379
    port: 6379
    #host: dev-elasticache-replication-group.ikmdkh.ng.0001.apn2.cache.amazonaws.com
    host: master.redis-cls-stg-sales-as1.txsqxp.apn2.cache.amazonaws.com
    #ssl:true 20260414에 추가함
    ssl: true

jdbc:
  #url: jdbc:log4jdbc:postgresql://10.10.5.183:5432/cenlotsdb02?charSet=UTF-8&prepareThreshold=1&serverTimezone=Asia/Seoul
  url: jdbc:log4jdbc:postgresql://db-cls-stg-mgmt.c3uywyy2okpb.ap-northeast-2.rds.amazonaws.com:5432/cenlotsdb02?charSet=UTF-8&prepareThreshold=1&serverTimezone=Asia/Seoul
  username: cenapp02
  password: cenlotapp123!
  driverClassName: net.sf.log4jdbc.sql.jdbcapi.DriverSpy

jdbcSales:
  url: jdbc:log4jdbc:postgresql://db-cls-stg-sales.c3uywyy2okpb.ap-northeast-2.rds.amazonaws.com:5432/cenlotsdb01?charSet=UTF-8&prepareThreshold=1&serverTimezone=Asia/Seoul
  username: cenapp01
  password: cenlotapp123!
  driverClassName: net.sf.log4jdbc.sql.jdbcapi.DriverSpy


management:
  endpoints:
    web:
      exposure:
        include: "*"  # 모든 Actuator 엔드포인트 활성화
eureka:
  client:
    register-with-eureka: false  # 자신을 Eureka에 등록하지 않음
    fetch-registry: false

cors:
  enabled: true


#eureka:
#  instance:
#    prefer-ip-address: true
#    hostname: cenlots2024-discoveryserver-service
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@${eureka.instance.hostname}/eureka/

